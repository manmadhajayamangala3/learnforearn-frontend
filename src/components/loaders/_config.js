// ─────────────────────────────────────────────
// LOADER CONFIG
// LOADERS_ON   : true = show loaders, false = skip all
// TEST_DELAY_MS: section/panel loaders — 0 = show content immediately when data arrives
// PAGE_MIN_MS  : full-page loaders — minimum ms to hold before showing page (1 animation cycle)
// ─────────────────────────────────────────────
export const LOADERS_ON    = true
export const TEST_DELAY_MS = 0     // panels/sections: instant
export const PAGE_MIN_MS   = 900   // full-page: just enough to see the loader
